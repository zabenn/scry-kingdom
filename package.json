{
  "name": "firefox-extension-template",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/contentScript.ts --bundle --outfile=dist/contentScript.js --format=iife && copyfiles -u 1 src/manifest.json src/*.html src/styles/*.css src/*.png dist/"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.13",
    "copyfiles": "^2.4.1",
    "prettier": "^3.7.4",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "scryfall-sdk": "^5.0.3"
  }
}
